import { AdminLogs } from 'dashboard'

// Admin-wide log table (fetches its own data). Static preview shows the screen
// chrome + column layout in its loading/empty state.
export function Default() {
  return <AdminLogs />
}
