import { SystemSettings } from 'dashboard'

// AI-system settings screen (fetches systems). Shows the settings layout.
export function Default() {
  return <SystemSettings customerId="demo-customer-0001" />
}
