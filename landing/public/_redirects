# Static text files — serve from origin, NOT the SPA
/robots.txt    /robots.txt    200
/llms.txt      /llms.txt      200
/sitemap.xml   /sitemap.xml   200

# Charter (static; versioned permalink resolves to the directory index)
/charter/v1    /charter/  301

# Privacy policy (static, canonical at trailing slash)
/privacy       /privacy/  301

# SPA catch-all (must be last)
/* /index.html 200
